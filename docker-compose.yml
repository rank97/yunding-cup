version: '3.8'

services:
  tft-tourneyos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tft-tourneyos
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - SPRING_PROFILES_ACTIVE=sqlite
